#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints the puts statement followed by a new line
 *
 * Return: always 0 (success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
